# 甜甜圈重渲染实测

实测批次：`20260906_015854_optimized`，2026-09-06 完成的历史测试。192 帧全部重新计算，8 秒、24 fps、960×800、24 samples，自适应阈值 0.06。GitHub 发布时沿用已有成片，没有重新运行本次测试。

## 耗时

| 项目 | 实测 |
|---|---:|
| 上次完整渲染（历史日志） | 约 746 秒 / 12 分 26 秒 |
| 本次 Blender 进程耗时（含启动、退出、监测轮询间隔） | 112.88 秒 |
| 本次逐帧循环耗时 | 108.21 秒 |
| H.264 编码 | 4.06 秒 |
| 本次总时间（含启动、渲染、编码、视频解码验证） | 118.33 秒 |
| 单帧中位数 | 0.544 秒 |
| 渲染耗时减少 | 84.9% |
| 相对上次的渲染速度 | 6.61 倍 |

这是与历史日志的比较，不是同一时刻的严格对照实验。画面尺寸、帧数和采样数保持不变，但降噪算法、数据复用、线程及进程调度一起调整，无法把改善单独归因于某一项。旧帧未用于新视频；OptiX 和原 OpenImageDenoise 的画面细节不会逐像素一致。

## 配置与负载

本机：Ryzen 7 5800H，8 核 16 线程；约 32 GB 内存；RTX 3070 Laptop GPU，8 GB 显存。检测时 NVIDIA 驱动版本 591.74，Blender 5.2.1 LTS。

| 指标 | 采样平均 | 采样最高 |
|---|---:|---:|
| Blender 占整机 CPU 比例 | 3.8% | 5.0% |
| 整机 CPU 占用 | 7.7% | 14.4% |
| 整机 GPU 占用 | 74.4% | 94.0% |
| 整机显存使用 | 3.81 GiB | 3.92 GiB |
| GPU 温度 | 64.5 °C | 69 °C |
| Blender 进程工作集内存 | 804 MiB | 820 MiB |

运行期间整机可用内存最低约 15.0 GiB。以上来自 51 次、约每两秒一次的采样，不包含视频编码阶段；采样可能错过瞬时峰值。GPU 指标包含桌面及其他应用，不是只统计 Blender。CPU 进程占用已除以逻辑核心数，可与任务管理器的整机比例比较。上次没有同口径负载记录，因此不提供虚构的 CPU 前后平均对比。

## 做了哪些优化

1. 仅启用 RTX 的 OptiX 渲染设备，CPU 光线追踪设备关闭。
2. 降噪从 CPU OpenImageDenoise 换为 GPU OptiX。
3. 开启 Persistent Data，让动画帧复用渲染数据。
4. Blender 渲染线程、OpenMP/MKL 上限设为 8，使用低于普通任务的进程优先级。
5. H.264 软件编码限制为 4 线程。

当前场景下，8 GB 显存和 32 GB 内存都有余量，暂时没有为此升级硬件的必要。没有修改驱动、电源策略、超频、系统服务或 Python 环境。新文件和渲染缓存全部写入 E 盘。

Blender 官方说明，Persistent Data 可以加快重新渲染和动画渲染，代价是保留更多内存；本次已实测内存余量。[官方性能说明](https://docs.blender.org/manual/en/4.1/render/cycles/render_settings/performance.html)。[GPU 渲染官方说明](https://docs.blender.org/manual/en/5.0/render/cycles/gpu_rendering.html)

## 验证与原始记录

- MP4 完整解码通过：192 帧、24 fps、8 秒、960×800。
- 渲染前后主工程 SHA-256 相同：`c5ed242a5ce3d61791257124d61eef63ee641c5a3b15c780b1297bc8a07150ff`。
- 视觉抽查记录：[45, 170]；自动解码验证不替代视觉检查。
- [公开实测摘要](data/render-benchmark.json)：保留渲染参数、耗时和资源统计，移除本机绝对路径。
- [发布前场景验证](data/scene-validation.json)：确认主工程的动画、时长与外部依赖。
- [成片与模型下载](https://github.com/CwC-HydeX/donut-studio/releases)。

逐帧耗时、CPU/GPU 原始采样与完整 Blender 日志仅保存在原电脑的 `reports/20260906_015854_optimized/`；首轮视频保留在 `archive/first-render/`。这些未随仓库发布。现有 `finalize_delivery.py` 保留原本机流程，再次运行时会重写本说明并生成本地交付包；发布后续修改前应重新检查文档链接。
