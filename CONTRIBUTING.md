# 贡献与提交约定

项目主创为 **CwC**，首批作品和文档由 **GPT-6 Astra（OpenAI Codex）**协助完成。授权范围见 [LICENSE.md](LICENSE.md)。

## 提交内容

按实际改动分组，例如仓库规则、建模、渲染、场景和文档。标题用中文说明结果，正文列出涉及的文件、变化原因、验证结果和已知限制。不要只写“更新文件”。

GitHub 文件列表显示的是最近一次涉及该文件的提交标题，不是独立文件备注。相关文件可以一起提交，稳定的用途说明放在 [项目文件导览](docs/项目文件导览.md)。

AI 实际参与的提交在正文后留空行，并写共同作者：

```text
Co-authored-by: GPT-6 Astra (OpenAI Codex) <codex@openai.com>
```

后续应填写实际使用的模型，仅人工完成的改动不添加 AI 作者。该标记记录工具参与，不表示代表 OpenAI 公司提交。多行提交说明保存为 UTF-8 文件后，用 `git commit -F 文件路径` 提交。

## 文件收录

- 收录主工程、制作脚本、四张预览、公共文档和不含本机路径的实测摘要。
- 不收录 `cache/`、`output/`、`archive/`、`reports/`、环境文件或安装迁移历史。
- 视频、高清图和单独模型通过 Releases 分发；不要每次把完整交付 ZIP 加入 Git。
- 现有模型约 1.57 MiB，使用普通 Git；本项目未启用 Git LFS。

## 验证

文档改动检查链接和预览图。模型或动画改动检查代表帧和依赖完整性；无需因普通文档更新重新渲染整个视频。制作脚本仍包含本机路径，相关限制见 [使用指南](docs/使用与渲染指南.md)。
